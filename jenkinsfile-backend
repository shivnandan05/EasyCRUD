pipeline {
    agent {
        label 'worker'
    }
    stages{
        stage('checkout'){
            steps{
                git branch: 'main', url:'https://github.com/shivnandan05/EasyCRUD.git'
            }
        }
    }

}